title = 'Basic CPushButton example'
description = 'This example shows how to create a display that allows setting properties in the control system ' \
              'via a button press. Push button has a predefined value that is sent into the control system whenever ' \
              'the button is pressed. The value is reflected in the label below. Both these widgets are ' \
              'ComRAD widgets (<code>CPushButton</code> and <code>CLabel</code>) that are connected to the same ' \
              'device/property.'

entrypoint = 'app.ui'
japc_generator = 'japc_device:create_device'
