title = 'Basic CEmbeddedDisplay example'
description = 'This example shows how to create a composite UI, which reuses parts of the interface defined ' \
              'in different files. While <code>CRelatedDisplayButton</code> can replace the entire window with a ' \
              'new file, this container allows embedding parts of it. Embedded displays can be configured with the ' \
              'macros mechanism (see <strong>2. Advanced</strong> examples).'

entrypoint = 'app.ui'
