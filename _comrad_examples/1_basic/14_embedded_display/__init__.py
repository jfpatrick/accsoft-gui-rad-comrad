title = 'Basic CEmbeddedDisplay example'
description = 'This example shows how to create a composite UI, which reuses parts of the interface defined' \
              'in different files. While CRelatedDisplayButton can replace the entier window with a new file,' \
              'this container allows embedding parts of it. Embedded displays can be configured with the macros ' \
              'mechanism (see Advanced examples).'

entrypoint = 'app.ui'
