title = 'Basic CCheckBox example'
description = 'This example shows how to create a display that allows setting boolean properties in the control ' \
              'system via a checkbox. <code>CCheckBox</code> sends a boolean value, represented by <code>0</code> or ' \
              '<code>1</code> in numeric format. The value is reflected in the label below. Both these widgets are ' \
              'ComRAD widgets (<code>CCheckBox</code> and <code>CLabel</code>) that are connected to the same ' \
              'device/property.'

entrypoint = 'app.ui'
japc_generator = 'japc_device:create_device'
