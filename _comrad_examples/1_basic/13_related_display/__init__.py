title = 'Basic CRelatedDisplayButton example'
description = 'This example shows how to create a button that opens a new display replacing the current window or' \
              'spawning a new external window. It does not require an control system configuration unless the ' \
              'secondary display requires the connection. Related displays can be configured with the macros ' \
              'mechanism (see Advanced examples).'

entrypoint = 'app.ui'
