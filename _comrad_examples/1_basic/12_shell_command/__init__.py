title = 'Basic CShellCommand example'
description = 'This example shows how to create a button that executes an external Unix command. ' \
              'It does not require a control system configuration, as the command/executable is found ' \
              'locally in <code>$PATH</code>.'

entrypoint = 'app.ui'
