title = 'Custom layout in CPropertyEdit'
description = 'This example shows how to create a <code>CPropertyEdit</code> widget with custom layout. By default, ' \
              'the <code>CPropertyEdit</code> will arrange its inner widgets in a form layout. However, you can ' \
              'define a custom delegate that will provide its own layout.'

entrypoint = 'app.py'
japc_generator = 'japc_device:create_device'
