title = 'Code CPropertyEdit example'
description = 'This example shows how to create a CPropertyEdit widget, similar to 1.17 (Property Edit), but ' \
              'using code this time. The JAPC device is exactly the same, and the main widget configuration is' \
              'identically. The second (read-only) property edit widget from the original example has been ' \
              'omitted for the sake of simplicity.'

entrypoint = 'app.py'
japc_generator = 'japc_device:create_device'
