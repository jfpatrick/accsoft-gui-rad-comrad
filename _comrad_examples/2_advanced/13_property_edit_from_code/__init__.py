title = 'Code CPropertyEdit example'
description = 'This example shows how to create a <code>CPropertyEdit</code> widget, similar to <strong>1.17. ' \
              'Property Edit</strong>, but using code this time. The JAPC device is exactly the same, and the main ' \
              'widget configuration is identical. The second (read-only) <code>CPropertyEdit</code> widget from the ' \
              'original example has been omitted for the sake of simplicity.'

entrypoint = 'app.py'
japc_generator = 'japc_device:create_device'
