title = 'Custom widget in CPropertyEdit'
description = 'This example shows how to create a <code>CPropertyEdit</code> widget with custom inner widgets.' \
              'By default, the <code>CPropertyEdit</code> picks a widget corresponding to the field type. However, ' \
              'you can define  a custom delegate that will provide its own widgets.'

entrypoint = 'app.py'
japc_generator = 'japc_device:create_device'
